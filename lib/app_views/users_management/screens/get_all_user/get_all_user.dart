
import 'package:flutter/material.dart';

class GetAllUser extends StatelessWidget {
  static const id = "get_all_user";
  const GetAllUser({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
    );
  }
}
